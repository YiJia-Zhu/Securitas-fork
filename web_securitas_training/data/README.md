# Data Placeholder

Place the processed Web/WF CW100 files for Securitas training here:

```text
data_2000.pkl
label_2000.pkl
```

The repository uses relative paths from `web_securitas_training/`, so the default training paths are:

```text
./data/data_2000.pkl
./data/label_2000.pkl
```
